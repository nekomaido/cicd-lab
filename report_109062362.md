# CI Pipeline 實作報告

學號：109062362

## 1. CI Pipeline 說明

本次作業在專案中新增 `.github/workflows/ci_109062362.yaml`，讓 GitHub Actions 在每次 `push` 到任一 branch 時自動執行 CI pipeline。

Pipeline 主要檢查三件事：

1. TypeScript typecheck：確認 TypeScript 型別正確。
2. Prettier check：確認程式碼與設定檔符合格式規範。
3. Test：執行 Vitest 測試，並產生 JUnit XML 測試報告。

三個檢查都使用 GitHub Actions 預設的失敗機制，只要任一 command 回傳非 0 exit code，該 step 會失敗，整個 pipeline 也會顯示 failed。

## 2. Workflow 主要內容

檔案位置：

```text
.github/workflows/ci_109062362.yaml
```

主要內容如下：

```yaml
name: CI 109062362

on:
  push:
    branches:
      - '**'

permissions:
  contents: read

concurrency:
  group: ci-109062362-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality-gates:
    name: Typecheck, format, and test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '24'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: TypeScript typecheck
        run: npm run typecheck

      - name: Prettier check
        run: npm run format:check

      - name: Run tests with JUnit report
        id: tests
        run: |
          mkdir -p reports
          npm test -- --reporter=default --reporter=junit --outputFile.junit=reports/vitest-junit.xml

      - name: Add test results to job summary
        if: ${{ always() && steps.tests.conclusion != 'skipped' }}
        run: |
          node <<'NODE'
          // 讀取 reports/vitest-junit.xml，將測試摘要寫入 GitHub Actions job summary。
          NODE

      - name: Upload JUnit test report
        if: ${{ always() && steps.tests.conclusion != 'skipped' }}
        uses: actions/upload-artifact@v7
        with:
          name: vitest-junit-report
          path: reports/vitest-junit.xml
          if-no-files-found: error
```

## 3. Pipeline 設計說明

此 pipeline 使用單一 job `quality-gates`，依序執行安裝依賴、型別檢查、格式檢查與測試。這樣設計的原因是希望越基礎的品質問題越早被攔下：

1. `npm ci`：使用 `package-lock.json` 安裝一致的依賴版本，避免 CI 與本機套件版本不同。
2. `npm run typecheck`：執行 `tsc --noEmit`，只做型別檢查，不輸出編譯結果。
3. `npm run format:check`：執行 Prettier check，確保 repository 中的 TypeScript、YAML、Markdown 等檔案格式一致。
4. `npm test -- --reporter=default --reporter=junit --outputFile.junit=reports/vitest-junit.xml`：執行 Vitest，除了在 log 顯示測試結果，也輸出 JUnit XML。
5. `Add test results to job summary`：解析 JUnit XML，將測試統計與 test case 狀態寫到 GitHub Actions 的結果頁面。
6. `Upload JUnit test report`：將完整測試結果上傳為 artifact，方便下載保存。

測試結果摘要與 artifact upload 都使用 `if: ${{ always() && steps.tests.conclusion != 'skipped' }}`，因此即使測試失敗，仍然會盡量保留測試報告，方便除錯。

## 4. CI 執行結果截圖

GitHub Actions workflow 頁面：

```text
https://github.com/nekomaido/cicd-lab/actions/workflows/ci_109062362.yaml
```

成功 run 頁面：

```text
https://github.com/nekomaido/cicd-lab/actions/runs/26023652925
```

成功執行截圖：

> [請在這裡貼上成功執行的 GitHub Actions 截圖]

GitHub Actions 結果畫面截圖：

> [請在這裡貼上成功 run 的 job summary 或 step 結果畫面截圖]

成功案例說明：

- Branch：`main`
- Workflow：`CI 109062362`
- 預期結果：`TypeScript typecheck`、`Prettier check`、`Run tests with JUnit report` 全部通過。
- 本地驗證結果：`npm run typecheck`、`npm run format:check`、Vitest 2 個測試皆通過。

## 5. 失敗案例說明

本次失敗案例使用「測試失敗」作為示範。

失敗案例 branch：

```text
demo/ci-failure-109062362
```

失敗 run 頁面：

```text
https://github.com/nekomaido/cicd-lab/actions/runs/26023703137
```

故意製造的錯誤：

```ts
message: 'Broken CI demo message';
```

原本測試期待首頁 `/` 回傳：

```ts
'CI/CD Lab Fastify app is running';
```

因此當 source code 回傳錯誤訊息時，`test/app.test.ts` 中的測試會失敗，GitHub Actions pipeline 也會標示為 failed。

失敗執行截圖：

> [請在這裡貼上 failed pipeline 的 GitHub Actions 截圖]

失敗原因：

- `GET / returns app message and version` 測試會檢查首頁回傳的 `message`。
- 失敗 branch 故意把 `src/app.ts` 的 `message` 改成錯誤文字。
- 實際結果與測試預期不一致，所以 Vitest 回傳非 0 exit code。
- GitHub Actions 因為 test step 失敗而將整個 pipeline 標示為 failed。

修正方式：

將 `src/app.ts` 的 `message` 改回：

```ts
message: 'CI/CD Lab Fastify app is running';
```

修正後重新 push，pipeline 會再次執行；當 TypeScript typecheck、Prettier check 與 Test 都通過時，GitHub Actions 會顯示成功。

## 6. 使用工具與策略

使用工具：

- GitHub Actions：自動執行 CI pipeline。
- `actions/checkout@v6`：下載 repository 程式碼。
- `actions/setup-node@v6`：安裝 Node.js 24 並使用 npm cache。
- `actions/upload-artifact@v7`：上傳 JUnit XML 測試報告。
- TypeScript：執行型別檢查。
- Prettier：檢查程式碼格式。
- Vitest：執行測試並輸出 JUnit report。

策略：

- 在 push 時自動執行，確保每次提交到 GitHub 都會被檢查。
- 將 typecheck、format check、test 放在同一個 job 中，讓任何品質問題都能直接讓 CI failed。
- 使用 JUnit XML、GitHub Actions job summary 與 artifact 保存測試結果，讓成功與失敗的測試資訊都能在 Actions 頁面中查看。
- 另外將專案既有 YAML 範例檔與 `docker-compose.yml` 格式化，確保 `npm run format:check` 在整個 repository 上可以穩定通過。
